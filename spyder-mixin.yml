# For using any environment with Spyder
###############################################################################
channels:
  - defaults
dependencies:
  - ipykernel >= 5.1.3
  - pyqt
  - spyder-kernels >= 1.8
