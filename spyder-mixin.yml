# For using any environment with Spyder
###############################################################################
channels:
  - spyder-ide
  - defaults
dependencies:
  - ipykernel >= 5.1.3
  - pyqt
  - spyder-kernels >= 1.4
